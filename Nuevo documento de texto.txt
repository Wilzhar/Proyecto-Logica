<script>
        // define 
        var dataReload = document.querySelectorAll("[data-reload]");

        console.log(dataReload);

        //Traducion de las palabrasde la pagina ingles o español
        var lenguje = {
          es: {
            validacionArgumentos: "VALIDACIÓN ARGUMENTOS",
            ingresoFormulas: "INGRESO DE FORMULAS",
            formulasValidadas: "FORMULAS VALIDADAS"
          },
          eng: {
            validacionArgumentos: "ARGUMENT VALIDATION",
            ingresoFormulas: "FORMULA ENTRY",
            formulasValidadas: "VALID FORMULAS"
          }
        };

        //Define el lenguaje con un window hash
        if(window.location.hash){
          if(window.location.hash === "#eng"){
            txtValidacionArgumentos.textContent = lenguje.eng.validacionArgumentos;
            txtIngresoFormulas.textContent = lenguje.eng.ingresoFormulas;
            txtFormulasValidadas.textContent = lenguje.eng.formulasValidadas;
          }
        }

        //Recarga la pagina al seleccionar un lenguaje
        for(i=0; i<=dataReload.length; i++){
          console.log(dataReload[i]);
          //dataReload[i].onclick = function(){
            //location.reload(true);
          //};
        }
      </script>